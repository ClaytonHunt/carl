name: Release CARL

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., v1.7.1)'
        required: true
        type: string

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.version }}"
          else
            VERSION="${GITHUB_REF#refs/tags/}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "version_number=${VERSION#v}" >> $GITHUB_OUTPUT

      - name: Validate files exist
        run: |
          if [ ! -f "install.sh" ]; then
            echo "❌ install.sh not found"
            exit 1
          fi
          if [ ! -f "update-carl.sh" ]; then
            echo "❌ update-carl.sh not found"
            exit 1
          fi
          if [ ! -d ".carl" ]; then
            echo "❌ .carl directory not found"
            exit 1
          fi
          if [ ! -d ".claude" ]; then
            echo "❌ .claude directory not found"
            exit 1
          fi
          echo "✅ All required files and directories found"

      - name: Update version in files
        run: |
          VERSION_NUMBER="${{ steps.version.outputs.version_number }}"
          echo "🔄 Updating version to $VERSION_NUMBER in files..."
          
          # Update install.sh version
          if grep -q "CARL_VERSION=" install.sh; then
            sed -i "s/CARL_VERSION=.*/CARL_VERSION=\"$VERSION_NUMBER\"/" install.sh
          else
            # Add version if not present
            sed -i '/^#!/a CARL_VERSION="'$VERSION_NUMBER'"' install.sh
          fi
          
          # Update update-carl.sh version  
          if grep -q "# Version:" update-carl.sh; then
            sed -i "s/# Version:.*/# Version: $VERSION_NUMBER/" update-carl.sh
          fi

      - name: Commit version updates
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          if git diff --quiet; then
            echo "ℹ️ No version changes to commit"
          else
            git add install.sh update-carl.sh
            git commit -m "Release ${{ steps.version.outputs.version }}: Update version numbers"
            git push origin HEAD:main
          fi

      - name: Generate changelog
        id: changelog
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          PREVIOUS_TAG=$(git describe --tags --abbrev=0 HEAD~1 2>/dev/null || echo "")
          
          echo "## 🚀 CARL $VERSION Release" > CHANGELOG.md
          echo "" >> CHANGELOG.md
          
          if [ -n "$PREVIOUS_TAG" ]; then
            echo "### Changes since $PREVIOUS_TAG" >> CHANGELOG.md
            echo "" >> CHANGELOG.md
            git log --pretty=format:"- %s (%h)" $PREVIOUS_TAG..HEAD >> CHANGELOG.md
          else
            echo "### Initial Release" >> CHANGELOG.md
            echo "" >> CHANGELOG.md
            echo "- Initial CARL release with installation and update scripts" >> CHANGELOG.md
          fi
          
          echo "" >> CHANGELOG.md
          echo "### 📦 Installation" >> CHANGELOG.md
          echo "" >> CHANGELOG.md
          echo '```bash' >> CHANGELOG.md
          echo "# Quick install in current directory" >> CHANGELOG.md
          echo "bash <(curl -s https://github.com/ClaytonHunt/carl/releases/latest/download/install.sh)" >> CHANGELOG.md
          echo "" >> CHANGELOG.md
          echo "# Install in specific project" >> CHANGELOG.md
          echo "bash <(curl -s https://github.com/ClaytonHunt/carl/releases/latest/download/install.sh) /path/to/project" >> CHANGELOG.md
          echo '```' >> CHANGELOG.md
          echo "" >> CHANGELOG.md
          echo "### 🔄 Update" >> CHANGELOG.md
          echo "" >> CHANGELOG.md
          echo '```bash' >> CHANGELOG.md
          echo "# Update existing CARL installation" >> CHANGELOG.md
          echo "/carl:update" >> CHANGELOG.md
          echo '```' >> CHANGELOG.md
          
          # Set output for release notes
          {
            echo 'changelog<<EOF'
            cat CHANGELOG.md
            echo EOF
          } >> $GITHUB_OUTPUT

      - name: Create release archive
        run: |
          echo "📦 Creating CARL release archive..."
          tar -czf carl-${{ steps.version.outputs.version }}.tar.gz .carl .claude install.sh update-carl.sh
          echo "✅ Archive created: carl-${{ steps.version.outputs.version }}.tar.gz"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: "CARL ${{ steps.version.outputs.version }}"
          body: ${{ steps.changelog.outputs.changelog }}
          files: |
            install.sh
            update-carl.sh
            carl-${{ steps.version.outputs.version }}.tar.gz
          # These files will be available as:
          # - https://github.com/ClaytonHunt/carl/releases/latest/download/install.sh  
          # - https://github.com/ClaytonHunt/carl/releases/latest/download/update-carl.sh
          # - https://github.com/ClaytonHunt/carl/releases/latest/download/carl-${{ steps.version.outputs.version }}.tar.gz
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update repository topics
        run: |
          # Update repository with release info
          echo "✅ CARL ${{ steps.version.outputs.version }} released successfully!"
          echo "📦 Release files are now available at:"
          echo "   - https://github.com/ClaytonHunt/carl/releases/latest/download/install.sh"  
          echo "   - https://github.com/ClaytonHunt/carl/releases/latest/download/update-carl.sh"